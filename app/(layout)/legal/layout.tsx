import type { LayoutParams } from "@/types/next";

export const ~/dev/indie/nowts/app/(layout)/legal = (props: LayoutProps<{}>) => {
  return 
}

import type { LayoutParams } from "@/types/next";

export const ~/dev/indie/nowts/app/(layout)/legal = (props: LayoutProps<{}>) => {
  return 
}